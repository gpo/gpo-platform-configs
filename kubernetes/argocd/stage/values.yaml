# genk8s:hash=57fa903f3cd6ebd92fcc49da6728152c
global:
  domain: argocd.gpotoolsstage.ca

configs:
  params:
    server.insecure: true
  cm:
    accounts.rsalmond: "apiKey, login"

server:
  httproute:
    enabled: true
    parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: gpotools
      namespace: gateway
      sectionName: tls

dex:
  enabled: false
