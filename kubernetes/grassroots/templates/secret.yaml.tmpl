# genk8s:encrypted=true
# this file is generated dynamically, hand edits will be overwritten

# this secret is for grassroots app
apiVersion: v1
stringData:
  .env.production: |
    POSTGRES_USER = grassroots
    POSTGRES_PASSWORD = ${postgres_password}
    POSTGRES_HOST = grassroots-rw
    POSTGRES_DATABASE = grassroots
    POSTGRES_PORT = 5432

    VITE_BACKEND_HOST = ${vite_backend_host}
    VITE_FRONTEND_HOST = ${vite_frontend_host}

    SESSION_SECRET = ${session_secret}

    GOOGLE_CLIENT_ID = ${google_client_id}
    GOOGLE_CLIENT_SECRET = ${google_client_secret}
    GOOGLE_AUTH_CALLBACK_URL = ${google_auth_callback_url}

    TEST_APPROVED_PHONE_NUMBER = 2125554202
    ENABLE_PHONE_CANVASS_SIMULATION = false

    TWILIO_SID = ${twilio_sid}
    TWILIO_AUTH_TOKEN = ${twilio_auth_token}
    TWILIO_OUTGOING_NUMBER = 2262412052

    TWILIO_API_KEY_SID = ${twilio_api_key_sid}
    TWILIO_API_KEY_SECRET = ${twilio_api_key_secret}
    TWILIO_APP_SID = ${twilio_app_sid}
    TWILIO_SYNC_SERVICE_SID = ${twilio_sync_service_sid


    WEBHOOK_HOST = ${webhook_host}

    VALID_LOGIN_EMAIL_REGEX = ^(tdresser@gmail.com|[+a-zA-Z0-9.]*@gpo\.ca)
kind: Secret
metadata:
  name: grassroots
type: Opaque

---

apiVersion: v1
kind: Secret
metadata:
  name: grassroots-postgres-db
type: kubernetes.io/basic-auth
stringData:
  username: grassroots
  password: ${postgres_password}
