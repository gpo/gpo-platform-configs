# genk8s:hash=b932248126fac530ece6ced5a3b76037
kind: HTTPRoute
apiVersion: gateway.networking.k8s.io/v1
metadata:
  name: superset
  namespace: superset
  labels:
    gateway: gpotools
spec:
  # this connects us to the gateway
  parentRefs:
  - kind: Gateway
    name: gpotools
    namespace: gateway
    sectionName: tls
  hostnames:
  - "superset.gpotoolsstage.ca"
  rules:
  - backendRefs:
    - name: superset
      port: 8088
