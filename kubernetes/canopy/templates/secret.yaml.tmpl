# genk8s:encrypted=true
# this file is generated dynamically, hand edits will be overwritten
#
# TODO: DB_NAME / DB_USER / DB_PASSWORD / DB_HOST are intentionally absent.
# No Cloud SQL instance exists yet for canopy (see CLAUDE.md's "Cloud SQL"
# commitment vs. what's actually provisioned) - add them here once that's
# decided in a follow-up. Until then this app cannot boot WordPress.
#
# TODO: WP_HOME / WP_SITEURL are also absent - they depend on the canopy
# Gateway's hostname, which isn't wired up yet either.
apiVersion: v1
stringData:
  .env: |
    WP_ENV = ${environment}

    # Generate real values with: https://roots.io/salts.html
    AUTH_KEY = generateme
    SECURE_AUTH_KEY = generateme
    LOGGED_IN_KEY = generateme
    NONCE_KEY = generateme
    AUTH_SALT = generateme
    SECURE_AUTH_SALT = generateme
    LOGGED_IN_SALT = generateme
    NONCE_SALT = generateme
kind: Secret
metadata:
  name: canopy
type: Opaque
