# nginx fronts the php-fpm container over the shared pod network namespace.
# This is the single source of truth for the config - the canopy image
# (gpo/canopy Dockerfile) doesn't run nginx or ship its own copy.
apiVersion: v1
kind: ConfigMap
metadata:
  name: canopy-nginx
  labels:
    app: canopy
data:
  default.conf: |
    server {
        listen 8080;
        server_name _;

        root /app/web;
        index index.php;

        client_max_body_size 64m;

        location / {
            try_files $uri $uri/ /index.php?$args;
        }

        location ~ \.php$ {
            fastcgi_pass 127.0.0.1:9000;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include fastcgi_params;
            fastcgi_read_timeout 60s;
        }

        location = /healthz {
            access_log off;
            fastcgi_pass 127.0.0.1:9000;
            fastcgi_index healthz.php;
            fastcgi_param SCRIPT_FILENAME /app/web/healthz.php;
            include fastcgi_params;
        }

        location ~* \.(?:css|js|map|woff2?|ttf|svg|ico)$ {
            expires 30d;
            access_log off;
        }

        location ~ /\.(?!well-known) {
            deny all;
        }
    }
